---
name: skill-health-audit
description: "【Skill 结构体检】体检skill、检查一下skill、会不会好用。只管单skill结构；验收走验收skill"
version: 1.7.0
author: 彬少
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [skill, audit, maintenance, references]
    category: skill-maintenance
---

# Skill 结构体检

用户说「检查一下 XX skill / 有没有开源必要 / 会不会好用」时，**先做结构体检，再谈价值判断**。结构不健康的 skill 谈开源/交付是空中楼阁——孤儿文件、残留章节、断裂引用会让读者翻不到内容、读到坏渲染。

本 skill 管**单个 skill 内部**结构健康。跨 skill 库级清理（重叠/零使用诊断）是 skill-library-audit 的职责，不在本清单内。

## 体检清单（按序执行）

### 1. 通读 SKILL.md + 全部 references + scripts

- **无效目标兜底**：指定的 skill 目录/frontmatter `name` 不存在或读不了 → 提示使用者重选有效 `skill_dir`（先 `search_files` 全库核 name），不开始体检——「缺 X」类断言前尤其必核，别把「没找到目标」报成「目标缺项」

- **评价/体检/验收类结论必须全目录通读后落笔**：只读 SKILL.md 正文的结论会被 deep 文件推翻——旧口径与跨文件矛盾（references 教用主流程明令禁的口径、脚本硬编码主流程声称「由 config 定」的值）只在全文件逐字对读时暴露；脚本要读主体逻辑，不能只列文件名和体量。确实只审部分范围时，结论里显式标注未读范围。
- `skill_view(name)` 拿 SKILL.md 全文 + linked_files 列表
- 逐个 `read_file` 每个 references 文件。**报 Binary 时用 python 读**：read_file 对部分正常 UTF-8 文件会误判 binary，不止 Unicode variation selectors（VS1-256）一种原因——纯中文 UTF-8 的 `.md`（无 VS、含【】等标点）也会触发（2026-08-09 实测 quote-classifier/dia-cdp-control/dia-bookmark-sync 三个 SKILL.md 被 read_file 误判 binary，但 `file` 显示纯 UTF-8、python 正常读出）。判别法：`file <path>` 若报 `UTF-8 text` 即正常，用 python 读出即可，不要被 binary 标记骗了：
  ```bash
  file <path>                                              # 看是否 UTF-8 text（而非真二进制）
  python3 -c "print(open('<path>','rb').read().decode('utf-8')[:1500])"
  ```

### 2. 引用完整性交叉检查（孤儿 reference 检测）——核心步骤

```bash
cd <skill_dir>
grep -oiE "references/[A-Za-z0-9_.-]+\.md" SKILL.md | sort -u   # 正文实际引用了哪些（与 audit 脚本正则逐字同口径：含点/大写/下划线/连字符）
ls references/                                             # 目录下实际有哪些
```

- **目录有、正文没引 → 孤儿**：内容写了但读者永远翻不到（skill_view 的 linked_files 是自动列目录，不代表正文引用）。处理：正文补链接，或删除，二选一，不留半吊子。
- **skill_view 的 linked_files 不算正文引用**——它是目录自动列举，只证明文件在，不证明正文挂了链接。孤儿判定以「grep SKILL.md 正文是否引用」为准（2026-09-12 实测：瘦身重排后 skill_view 显示 9 个 linked_files，脚本仍报 3 孤儿——文件都在，是重排丢了正文链接句）。
- **脚本的「N 个引用」≠附属文件总数；扫描口径 = 正文出现 `references/<文件名>.md` 字面即计入（链接语法/行内反引号/裸文本均命中——2026-09-22 负例实测，旧表述「仅覆盖 markdown 链接语法」不实已纠偏），但 scripts/ 目录与不带 references/ 前缀的提及不扫**，会漏检 scripts/ 孤儿。核对法：`find <skill_dir> -type f` 列全部附属文件，逐个 grep 正文提及次数，0 次才是真孤儿——引用数与文件数的差额本身就是待人工定性清单。
- **正文引了、目录没有 → 断裂引用**：补文件或删引用。
- **跨 skill 引用 ≠ 断裂（2026-09-22 立）**：正文引的文件本 skill 没有、但在**本库别的 skill 的 references/ 下存在**（正主在别处）→ 脚本自动识别，输出 `ℹ️ 跨 skill 引用（非问题）` 并给出正主路径，**不计入 problem、不影响退出码**。判断边界：**全库任何地方都找不到**才算真断裂。此前靠人工给每一处加「跨 skill 文件」措辞来消红（acceptance/report-schema/upstream-contribution 已改过 3 次），第 4 次遇到改为工具侧识别——同族假红的正确处置是改口径，不是逐处改文件。
- **正则字符类漏点 = 假孤儿 + 假断裂双报**：引用检测的字符类必须含 `.`（`[A-Za-z0-9_.\-]+\.md`），否则带版本号的文件名（`v0.20.5-docs-audit-2026-08.md`）只匹配到尾段（`08.md`），正文明明引了却同时报「孤儿」和「断裂」。见到「引用了却报孤儿」先怀疑扫描口径，别先改文件——改口径一处，全库同类假红一起消失。
- **断裂报告先三分类定性再动手**（2026-09-06 全量验收 19 处断链实测：真断裂仅 3）：①示例代码块里的路径（教格式的举例）→ 不修；②举例性提及（教学句里写「坑表可写成独立 common-pitfalls.md 文件」这类，不带路径前缀也不算引用）→ 不修；③跨 skill 文件引用（正主在别的 skill 里且文件存在）→ **脚本已自动识别为 ℹ️ 非问题（2026-09-22 起：全库查正主），不再需要人工加措辞消歧**（旧法已废——同族假红的正确处置是改口径，不是逐处改文件）。只有「指向本目录 references/ 但本库任何 skill 都没有此文件」才是真断裂，脚本正则分不了 ①②，人工复核必做。
- **核对口径补充**（原 skill-library-audit「单 Skill 内部健康审计」第 7 步，2026-09-20 归位）：③类跨 skill 引用**去归属 skill 的 `references/` 核对**——按本目录报断链是**扫描器口径错误**；这条核对手法现已内置进脚本（`ℹ️ 跨 skill 引用 → 正主 <路径>`），人工只在脚本找不到正主却又确信该文件存在时介入。
- 2026-08-08 实测：某多文件 skill 目录 9 个 references，正文只引 4 个，5 个孤儿（其中 4 个有货只是没挂链接、1 个是错位文件）。

### 2b. 同名副本遮蔽检测（CLI 按 name 去重的盲区，2026-09-06 实测）

**机制**：Hermes 按 frontmatter `name` 去重加载 skill——同一个 name 存在两份时（如官方位 `note-taking/obsidian` + 自建位 `<分类>/<主题>/<skill>`），CLI 列表只显示一份，**另一份永远不被加载**：它的 description 不进系统提示、内容从不生效，纯占磁盘。这解释了「为什么自建的增强版从未起作用」。

检测法（审计时对可疑 skill 跑一遍）：

```bash
# name 重复 = 有遮蔽（⚠️ 用 --include 递归：三层深的 <分类>/<主题>/<skill>/ 会被两层 glob 漏报）
# ⚠️ 噪音源：正文里 name: 开头的示例行（design-md 等有）——（--include=SKILL.md 精确匹配文件名，
#    .bak 不会混入，2026-09-22 对照实测纠偏）命中后先人工核对是否真为两份同 name 的 skill 目录再定性，勿拿计数直接当结论
grep -rh "^name:" ~/.hermes/skills --include=SKILL.md 2>/dev/null | sort | uniq -d
```

**处置（留增强、去遮蔽）**：diff 两份定谁含增强内容 → 增强版迁入「被 CLI 实际加载的那份」的位置 → 删被遮蔽副本。若加载位是官方 bundled（修改自动进 `list-modified` 受保护，`hermes skills reset --restore` 可随时回官方版）；若增强版是 user-owned 原作，反方向迁回自建位。迁移完成后**合并官方 frontmatter 字段（version/license/原作者 author）与增强正文**：author 保留原作者署名只追加适配者，版本号 bump（如 1.0.0 → 1.1.0）标记非官方原版——丢弃官方字段会丢失版本管理线索。

**注意**：curator 使用统计的 key 也按 name 记账——被遮蔽副本的使用数据会记到生效那份头上，判断「谁在用」时勿被误导。

### 2c. 散文指路语失效检测（外迁/重组/合并后必做）

**症状**：内容外迁到 references 后，正文里**用自然语言指路**的句子断头——「见第 X 节」「见批量处理第 N 条」「C 线工作流第 N 项」「见 Step 5 去重判据」「section above」，以及**编号索引型**——「查 X 文件的对应条目：①②③④⑤」「（1）（2）型清单」。这类句子不是 markdown 路径引用，第 2 步的 `references/<文件名>.md` grep 与孤儿/断链脚本**都扫不到**（脚本口径只覆盖链接语法与裸路径；本行原写的占位示例「references/ 前缀 + xxx 文件名」会被脚本自身正则当成一条真引用而报断裂——教学占位符一律用 `<文件名>` 写法，2026-09-22 消假红）；编号索引型更隐蔽：编号自身不带节名，外迁/搬迁后编号与实体脱钩（实测 2026-09-22：某 skill 异常表 ①-⑤ 中 3 条在所指文件里根本没有对应条目，且被指文件自述「会指向本文件条目编号」而全表无任何编号）。

```bash
grep -nE "见第|见 *Step|第 *[0-9]+ *(条|项|步|点)|上文|上节|前文|先见|section above|见下方|见前|对应条目[：:]|[①②③④⑤⑥⑦⑧⑨⑩]" <skill_dir>/SKILL.md <skill_dir>/references/*.md
```

逐条核对指路目标还在不在本文件：
- 目标是**本文件内的节/步骤** → 查该节是否仍存在（外迁常常把它一起带走）
- 目标**已外迁** → 改成带文件链接的显式引用（`[名称](references/<file>.md)`）或删句
- **编号索引型** → 逐个编号对被指文件核实体：编号在、实体缺 = 断头（改指真权威或补齐）；被指文件自称有编号制而实际无编号 = 该自述本身是失效声称，一并改
- **写引用时逐字复制目标标题**——引号里的节名/条名必须能在目标文件里 grep 到；转述成近义短语就是自造假指路（读的人搜不到，也不知道你原意）。自查法：把引号内字串回 grep 目标文件，零命中即你刚在造假目标（上一条查别人留下的存量，本条查自己刚写下的）
- **首次出现处**必须补显式链接，其后重复处可保留简短措辞

**为什么必查**：外迁时正文保留「流程骨架」，最自然的写法就是「详见第 N 条」——而那条恰恰被搬走了，执行者顺指路语翻不到内容，等于断头。实测（10 个 skill 批量瘦身）：路径引用层全绿、脚本零报错，系统性扫指路语才抓出 4 处悬空，且是用户追问「确定都没什么问题么」才动手查的——**外迁后主动扫，别等追问**。

### 3. 错位文件检测（属于别的 skill 的文件）

看到内容主题与本职不符的 reference（如记忆维护 skill 里出现 path-simulation 方法论）→ 去目标 skill 核实是否已有正主：

- 正主已有（目标 skill 的 SKILL.md 或 references 已含该内容）→ 冗余副本，删
- 正主没有 → 内容**迁回目标 skill** 再删副本（见第 6 步迁移检查）

- 2026-08-08 实测案例：`path-simulation-methodology.md` 混进 某多文件 skill 的 references（内容属别的 skill），核实正主后删副本
### 4. 重复/残留章节检测

```bash
grep -n "^## " SKILL.md          # 列所有二级标题，肉眼找同名
grep "^## " SKILL.md | sort | uniq -d   # 或直接找重复（⚠️ 不能带 -n：行号前缀使每行唯一，uniq -d 恒零输出）
```

- 同名章节出现两次 → 常见于迭代中粘贴残留：一个完整版 + 一个半成品
- 残留段常带**未闭合代码块**，会破坏后续整节渲染 → 必删
- 2026-08-08 实测：某多文件 skill 的「反模式与陷阱」出现两次，第一次是残缺散列表 + 孤立 \`\`\`，把后面「增量补记去重」整节渲染成代码块

### 5. 代码块配对检查

```bash
grep -c '^```' SKILL.md    # 应为偶数
```

- 奇数 → 有未闭合代码块，后续整节被渲染成代码 → 定位修复

### 6. 删除前迁移检查（迁移后删，不是删了再说）

被删文件里的独有信息是否在别处？

1. 在你的知识库/参考库中全文搜索关键词（有本地搜索工具用工具，没有用 grep）
2. 查目标 skill 的 references 是否已含
3. **独有 → 先迁到正主**（参考库对应文件，或目标 skill 的 references），**再删副本**
4. 不迁直接删 = 信息永久丢失
- 2026-08-08 实测：binshao-vault-conventions.md 里「Z｜附件按文章名建子文件夹」规则参考库没有，先补进 vault-architecture 的 vault-structure-conventions.md，再删记忆维护里的副本。

### 7. 权威声称核实（references 引用的外部权威是否真实存在）

references 里出现「X 文件第 N 行规定 Y」「config 定了 Z」这类**指向外部权威的声称**时，逐条 grep/read 验证，不能信声称：

- 声称的权威文件是否存在、那一行是否真是那个内容
- 2026-08-08 实测：usability-self-test.md 声称「MEMORY.md 硬上限 10k（SOUL.md 第22行定的规矩）」——实测 SOUL.md 第22行是「删除文件前必须问」，根本没有 10k 规定；真权威是 `config.yaml` 的 `memory_char_limit: 10000`。SKILL.md 第293行同款错误说法也中招。
- **为什么实战暴露不了**：主干流程只走「通读→决策→执行」，永远不会去核对 references 声称的权威——只有专门走跨文件路径才抓得到。
- **反向同样成立：自己下判据前先核源，别凭印象定标尺**。设阈值/上限类判据（字符上限、体积线、超时数）或做「某类全都缺 X」的断言前，先 grep 源码/权威文件拿实值，再按权威口径分区统计。实测两连翻车：① 按印象把 description 上限当 120 字符（源码 `SKILL_PROMPT_DESC_LIMIT = 60`，宽了一倍，超长项漏修一半，超限的触发词继续被截断）；② 凭错误分区断言「41 个 skill 缺 version」（按归属分区后自建区 100% 有，真缺失量为零，差一步去改官方区）。**印象值和错分区都会让整批操作打偏，而它们在报告里看起来同样言之凿凿——先拿命令输出再开口。**

### 7b. 可执行契约核实（带 scripts/ 的 skill 必做，与 7 互补）

第 7 步核的是「文档声称的外部权威是否真实」；本步核的是**文档声称的脚本行为是否真实**——只读文档永远验不到这一层，因为脚本对自己的描述也是一条待核实的声称。

```bash
# 逐个 flag 跑，退出码单独取（别用 | head 后读 $?——那是 head 的退出码，会把失败静默报成 0）
out=$(python3 <skill_dir>/scripts/X.py --flag 2>&1); code=$?
printf 'EXIT=%s\n%s\n' "$code" "$out"
```

- **文档给了退出码语义表 → 每个码都造一个输入显式触发一次**。没触发过的码是未测声称，不是「没触发所以没问题」。
- **反查 flag 互斥/叠加关系**：文档说「可叠加」就实跑组合；曾经出现过 5 个分析 flag 塞进 mutually_exclusive_group、而工具清单并列陈列，把「工具清单即契约」误导成「只能选一个」。
- **副作用守卫要跑「失败场景 + 副作用」**：声称「报错时不落盘」就造失败场景，比对文件 mtime/大小变没变——声称与实际副作用不一致时，文档在教一个假的容错路径。
- 详细方法论与派单模板见 [doc-fact-verification](../doc-fact-verification/SKILL.md)（可执行制品：跑而不只读 + 置信度边界标注）。

### 8. 兜底前向引用检查（异常解法与触发点距离）

skill 内部已知某步可能出错、解法写在文件尾部异常表时，检查**入口处有没有前向提示**：

- 触发点（如第一步 read_file 读三文件）与兜底（如异常表「三文件 read_file 报 Binary」）距离过远 → agent 走到触发点不知道有兜底，可能卡住
- 修法：在触发点加一行「若报 X，见下方异常处理表第 N 行兜底」
- 2026-08-08 实测：某多文件 skill 第一步大概率触发 Binary 误判（MEMORY.md 22601 字节含 VS 序列），兜底解法只在 299 行异常表，第一步（68 行）无提示

### 8b. 文档与脚本一致性检查（doc/script 割裂）

skill 带 `scripts/` 时，SKILL.md 描述的工作流必须和脚本实际能力对得上——两者割裂是常见但体检清单上最容易漏的一项（2026-08-09 实测 dia-bookmark-sync：SKILL.md 通篇「直接操作浏览器本地 JSON 文件、改完重启生效」，但目录下 3 个脚本全是 HTML 解析器——正则匹配 `<DT><A HREF=`，usage 是 `bookmarks-合并-YYYY-MM-DD.html`，SKILL.md 自己兜底承认「当前版本基于 HTML 格式」）。读者照文档走 JSON 工作流，脚本却吃不了，等于断头。

检查法：
```bash
ls <skill_dir>/scripts/                       # 有哪些脚本
head -25 <skill_dir>/scripts/*.py             # 看真实输入输出格式（HTML? JSON?）
grep -nE "用法|usage|\.html|\.json|HREF=|Bookmarks" <skill_dir>/SKILL.md
```
- 文档说 JSON、脚本吃 HTML（或反之）→ 割裂。两种修法二选一：脚本改读文档格式，或文档改回脚本真能吃格式。不准留「注：当前版本基于 X 格式」的兜底 — 那是把缺陷写进文档。
- 同时看脚本的 agents/openai.yaml、references/ 是否也被 SKILL.md 引用（回到第 2 步交叉检查）。
- 延伸坑见 `references/doc-script-split-pitfalls.md`——含 8b 延伸（脚本相对路径、config.json 死配置）与 **8d 延伸：方案/plan 类文档的权威声称检查手法**（体检对象是 plan 时必读，该文件为延伸节的权威存放处）。

### 8c. 触发词覆盖检查（description 是唯一的系统级触发面）

**机制（源码口径，勿再凭直觉改回）**：系统提示的 skill 索引只注入 frontmatter 的 `description`（`agent/prompt_builder.py` → `extract_skill_description`）；加载器读取的条件字段只有 toolset/platform 门控（`_CONDITION_KEYS`），**任何位置的 `triggers` 字段（顶层 `triggers:` 和 `metadata.hermes.triggers`）都不被读取，是死配置**。description 不含的触发词 = 该场景永远匹配不到。

**⚠️ description 还有长度截断（8c 的关键限定，勿漏）**：`SKILL_PROMPT_DESC_LIMIT = 60`（`agent/skill_utils.py`），索引只渲染 `description[:57] + "..."`——**超 60 字符的部分每轮对话都读不到**，触发词写在后半段 = 等于没写。所以触发词必须落在**前 57 字符内**（写法规范：职责一句话 + 最高频 3-4 词，见 skill-creation-rules 第三步）。**压缩到 ≤60 时，用户真实会说的入口词必须留在前 57 字**——用「流程与清单」顶掉「上传」这类原话 = 覆盖失败；`description-length` linter 绿 ≠ 触发覆盖过。低频问句不必硬塞 60 字，主路径词丢了就是病。截完用用户原话走一遍触发覆盖，不能拿上轮「结构 0 问题」当基线。

检查法：

```bash
python3 -c "import yaml,sys;s=open(sys.argv[1],encoding='utf-8').read();d=yaml.safe_load(s[3:s.find(chr(10)+'---',3)]);print(len(d['description']),d['description'])" <skill_dir>/SKILL.md
# >60 = 后半段触发词不进系统提示，按上表重写
```


```bash
grep -n "triggers:" <skill_dir>/SKILL.md           # 发现死 triggers 字段（不要往里补词）
grep -nE "「.*?」|用户说|触发" <skill_dir>/SKILL.md   # 正文里的触发场景词
```

- 正文写了触发场景、description 没覆盖 → 把词补进 **description**（模型只看得到它）。
- 发现 `triggers:` 字段 → 把里面的词并入 description 后**删掉字段**——留着会误导后续维护者以为它在生效。
- 双模式/多入口 skill 尤其要查：每个模式的入口词都要在 description 里。
- **合并来源 skill 时，源的专属触发词要并入 target 的 description**（如「发布到GitHub/公开仓库」「大规模蒸馏」这类词）——漏并 = 合并后源场景触发不到任何 skill。

### 8d. 架构改造后旧口径对账（版本升级/双模式改造后必做）

skill 做架构级改造（如 v5.x 引入双模式、改名、换方法论）后，**旧口径会残留在改造时漏掉的位置**——这是 8c 之外最常见的新坑，且路径模拟不一定抓得到（主干路径走通了，残留句只在旧表述里）。2026-08-10 实测 某多文件 skill v5.0→v5.2 双模式改造暴露三类：

```bash
grep -nE "旧模式名|旧关键词" <skill_dir>/SKILL.md <skill_dir>/references/*.md <skill_dir>/scripts/*.py   # 列出改造前的旧词全量对账（含脚本 print 输出与注释）
# 对账范围含库内消费方：被编排/被引用的清单，改动后 grep 全库找引用它的 skill 一并核（见坑表「姊妹 skill 动词/硬数字越界」）
grep -rln "<本 skill name>" ~/.hermes/skills --include=SKILL.md
```

1. **旧表述残留**：触发段落还写「必须走完整审计」（与新增的轻量模式矛盾）——把「完整审计」降级为仅重度模式用词，轻量触发句改「走审计流程（默认轻量）」
2. **标注漏改**：多个同类元素（如 3 处 CHECKPOINT）只改了前两处「重度保留，轻量并入方案」，第三处还是旧格式——**同类元素逐个 grep 标题确认全部标注一致**，不能「记得改了两处就收工」
3. **实现与说明脱节**：query 统一加了 `sort="newest"`，旁边说明文字还写「limit 是 FTS 相关性 topN」——实现改了、解释没同步，读者/执行者按旧说明理解
4. **脚本输出文字与注释残留**：砍表/改名后，旧表编号与旧名会留在脚本的 `print` 输出文字和注释里——这是「跑起来才看得见」的残留，读文档 grep 抓不到，但执行者跑脚本时会拿着旧编号去翻一个已经不存在的表。**对账必须真跑一次脚本看真实输出，grep 范围含 `scripts/*.py`（print 与注释都是藏身点）**；只「体检读一遍主文件 + references」会漏掉脚本层的断链。

**对账方法**：改造完成后，列出「旧概念关键词」（旧模式名、被废弃的限定词如「全量/已降级/必须通读」、旧参数语义），全目录 grep 逐条确认已消除或已改写；再对「重复出现的同款元素」（CHECKPOINT、警告行、示例 query）逐个核对是否都带新标注。此步在路径模拟之前做，可减少路径模拟的噪音。

### 8e. SKILL.md 体量分层检查（臃肿 ≠ 结构病）

体量超阈值（SKILL.md > ~15KB 或行数同类最大）但结构健康（0 孤儿/0 重复/配对完好）时，病灶通常是**「每次触发全文加载」**而非乱堆——判断口径：臃肿的真实代价是每次烧 token，不是看着乱。

1. **先统计各节行数占比**：主流程 vs 附则群。**附则群 = 纪律 / 陷阱 / 异常表三类（2026-09-22 钉口径）**——**触发条件与入口模式属「主流程入口」**、**参考文件索引是外迁 recipe 的产物**、**核心判据骨架（如三文件分工表）属「主流程不因瘦身砍内容」的保留对象**，三者都不计入附则。分母之争实证：同一对象（某多文件 skill，163 行）按宽口径算 = 50%（越线）、按本口径算 = 35/163 ≈ 21%（不越线），结论相反——**先钉口径再算占比，别拿宽口径的分母去触发外迁**。附则占比 >40% = 典型可外迁形态
2. **附则外迁 recipe**（2026-08-28 实测 某多文件 skill：444 行/43KB → 365 行/14KB，-67%）：同主题附则合并成 1-2 个 references（如 writing-disciplines.md + pitfalls.md）；SKILL.md 原位置留「⚠️ 必读指向行」（写明触发场景：何时必须去读）；外迁后 8e 自身的坑——**指向行替换原文时会连带删掉附近对旧 references 的引用语句，产生新孤儿**，改完必须重跑第 2 步孤儿检测
3. **主流程不因瘦身砍内容**：七步/决策树等主干骨架原地保留；瘦的是「规则细则」不是「流程步骤」。**外迁后禁止把主文件改成「动手前先读 references 全文」**——那把按需加载变成每次强制二次加载，弱模型会把细则当闸门；指向行只写触发场景（何时读哪一段），不写先读完再动手
4. **双表漂移检查**：SKILL.md 里维护的副本表（频率表/映射门表）要与权威源逐行核对——权威源加了行、副本没跟是高频漂移点（实测：README 频率表加 2 行，skill 副本漏 1 行）
5. **本 skill 自身的量化裁决（2026-09-27 复核，防「阈值越线就默认要外迁」）**：本 SKILL.md 已超本节 ~15KB 动作线，但按本节钉的分则口径重算——主流程 1-8h 占 61.7%，附则群（9b 有效性体检 + 陷阱群）仅 14.9%，即便把第 9 步与验证脚本节全算进附则的宽口径也只有 37.1%，**两种口径都不过 40% 线 → 不外迁**（占比为本轮实测，具体字符数会随增删漂移，此处不复写——**注记里写死体量数字就是本 skill 自己 8g 形态 4 的活标本**；发布副本经通用化替换后长度还会与本地不同）。**这说明「超 15KB」本身不是外迁判据，分则占比才是**；阈值只负责触发统计，不负责触发动作。体量本身的真实成本是每次触发的 token 消耗，与结构健康度无关——本 skill 的应对是分则按需读（`grep -n` + `sed -n`），不是外迁。

### 8f. 第三方仓库：体检对象是 canonical SKILL.md

目标是 GitHub 仓库而不是本地 `skill_dir` 时，**仓库文件数 / star 不是结构指标**。病毒 skill 常见形态：一份 <10KB 的 `skills/<name>/SKILL.md` + 十几个运行时适配器（`.claude-plugin/`、`INSTALL.md`、hooks）。适配层是分发工程，不进本 skill 的孤儿/臃肿口径。

1. 先定位 canonical 路径（仓库 AGENTS.md / README 会写 source of truth）
2. 只对那份 SKILL.md（及它声明的 references/）跑第 2–5 步
3. 仓库根的 marketplace 清单、多语言 README、always-on hook **不当作 skill 膨胀**

### 8g. 全量判据对账（不依赖本轮变更范围，抓跨文件语义打架）

**与 8d 的分工**：8d 的旧口径对账 grep 的是**本轮变更关键词**——历史轮次改造留下的旧口径不在本轮变更集里，关键词 grep 天然扫不到。8g 补的正是这个盲区：全目录、全量、按判据配对。2026-09-16 实证（某多文件 skill）：四轮验收（增量轮）全绿，同日全量深读抓出 3 处「SKILL.md 禁用 X 口径、某 references 教用 X 口径」打架（file_size/wc -c 计算口径、USER.md Binary 误判范围、P0/P2 旧分级）——全是历次修复漏改的存量，躺在 deep 文件里。

**方法（半机械：脚本出候选，人做定性）：**

先跑 `python3 <本skill目录>/scripts/criteria_overlap.py <skill_dir>`——自动提取**跨文件重复 token**（行内代码 token、数值阈值、计数点名、步骤/闸门号引用四类）并列出出现在 2+ 文件的候选对；脚本只出线索，误报率不低（示例词、泛用词），定性永远是人。对照实测（2026-09-16）：对人工判定无打架的 path-simulation 出 6 候选全假阳性（`NUM:3条`、`REF:步骤` 类同源泛词），对 某多文件 skill 出 41 候选含真打架线索——**候选信噪比低是设计内**：FP 是看一眼即判一致的无害候选，本步真正要防的是漏报，勿因 FP 弃用或收紧规则。无脚本环境（第三方安装缺 scripts/）按下列七形态手工枚举——**前六形态各来自 2026-09-16 四轮验收实战，每形态至少抓到一处真打架；第七形态来自 2026-09-22 本 skill 自身独立验收（P1/P2 两案）**：

1. **命令/CLI 参数与字段名**（如 `file_size`、`wc -c`、`--write-baseline`）——A 文件禁用、B 文件教学
2. **数值与阈值**（容量线、百分比、字符上限、超时数）——两处数值不等或口径（字节/字符）不同
3. **范围/义务声明**（必做/禁用/正常/可跳过/豁免/默认）——同一动作一边必走一边可跳
4. **计数点名与枚举**（「X 项/X 件套/X 连搜」+ 点名清单）——宣称数 ≠ 实列数、同名不同列
5. **跨文件节点/步骤号引用**（「步骤 X」「闸门 N」「第 N 条」）——被引侧重排/改名后失配
6. **外部权威声称**（「X 文件第 N 行」「源码 :1434」「config 定了 Z」）——行号漂移、行为改版（与第 7 步权威声称核实共用手法，7 步查单点真伪、本查查多处一致）
7. **能力/行为声称**（「脚本能查 X」「接口行为 Y」「工具支持 Z」）——只核存在性会放过硬声称假行为：2026-09-22 本 skill 自身两案，「frontmatter 完整性检测」实为只查 --- 开头、「传目录静默不出结果」实抛 IsADirectoryError。判法：对每条能力声称造一个负例喂进去（缺字段的 frontmatter、故意传错的参数类型），行为与声称对得上才算真。**⚠️ 造负例前先核该工具能不能被指到副本**：没有路径/根目录参数的工具点不了沙箱，实跑即写真实数据（实测：`MemoryStore` 签名无路径参数，`store=MemoryStore(...)` 看着像沙箱、实际写进了用户真实记忆文件；同族脚本 argparse 无路径参数则固定读写真实目录）——这类工具改**读源码定论**（`inspect.signature` / `inspect.getsource` 取事实），非跑不可时 cp 数据到临时目录 + 进程内 patch 模块路径常量，**绝不对真实目录加任何写 flag**。派子代理跑负例时，任务书必须显式带「禁调 X 的写路径」闸门——子代理默认「验行为 = 真跑一遍」。

只在单文件出现一次的判据不进对账（无打架对象）。同名判据逐对**双向**比对方向与数值：A 处说允许、B 处说禁止 = 必有一错或缺边界裁决；同时核对数值/范围是否一致。**必含 SKILL.md 主流程 ↔ references 双向**——执行者先读正文再跳参考文件，两边打架最致命（参考文件的旧口径会被当成正文细则的例外执行）。

**定性**：冲突项出「谁对谁错」结论（以实测记录/最新版本为准，参照 8d 的新旧判型），合法历史残留（版本演进注记、案例叙述）标注豁免理由。

**触发档位**：全量验收（scope=全量）、大版本/架构改造后、或修复引入「新口径替代旧口径」时必做（双版本并存迹象 = 触发信号）；**含多参考文件时增量轮亦必走**（对象可缩为「本轮新口径 vs 全目录存量判据」配对，成本可控）；确要跳过（仅限单参考文件以下的对象），须在报告「覆盖」行显式标注「8g 未跑」。**本节为 scope 判据唯一权威；skill-acceptance Phase 0 条 3 在其上加编排侧从严信号，不另立判据**。**编排侧加严信号已于 2026-09-22 撤销**：曾由 acceptance 加「references ≥5 个自动升全量」，实测反噬（某多文件 skill 16 个 references → 每轮自动重跑全量、30+ 调用 / 5-25 分钟）；**references 数量不是全量触发信号**，只决定增量口径下的配对范围（本轮新口径 vs 全存量判据）。

**获取判据对的方式（2026-09-22 提速，防「全量 = 通读全部」）**：形态 1/2/4/5 → 先跑 `criteria_overlap.py` 出候选，**只读命中涉及的文件**（某多文件 skill 16 个 references 实测只需读 3-4 个）；形态 3/6/7（范围义务 / 外部权威 / 能力行为声称）脚本提取不到 → 用 **lure 正则**定向抓。**lure 正则的权威副本是 `scripts/preflight_check.py` 里的 `LURE` 字典，本文档不复刻其内容**（双份维护必漂——2026-09-27 实测：正文抄的副本少了 `文件第` 与 `实测` 两项，照文档 grep 会少抓。改 lure 只改脚本一处）。`preflight_check.py` 的「8g 下一步线索」段会直接打印当轮实际生效的正则。**收工锚是「每条判据对有结论」，不是「每个文件都逐字读过」**——逐字通读只在两个场合恢复：大版本手术、发布前的全量档（2026-09-22 实测：全量通读 某多文件 skill 5.4 万字符 = 30+ 调用）。

**收工判据**：每条判据对有结论（一致 / 冲突已修 / 合法残留已标注）。

> 与 path-simulation 的分工：其跨文件路径 #7 是本步的走查版（执行定义已回指本方法）；只装本 skill 时 8g 独立可跑，无前置依赖。

### 8h. references 清理：先定性归属，再谈压缩（提「删/合并 references」前必做）

用户会问「这些看起来用不上了，删掉？」。**默认它们全是实跑撞出来的经验，不是过时文档**——
本 skill 自己的 `common-pitfalls.md` 有 45 条、2.7 万字，每条都是真实缺陷留下的判据，
其中若干条直接对应用户的核心痛点（如「异常表兜底措辞变成默认保留免问令」= 检出了问题却从不出方案）。
**凭体量或「过时」印象提议清理，会永久销毁一次教训。**

**🔴 禁止未打开文件就提清理方案。** 报错「你确定要清理」时的正确反应是**先统计再定性**，
不是解释为什么该删。统计口径（三条命令拿全）：

```bash
# 1. 体量分布：看是不是少数文件吃掉大部分
for f in <skill_dir>/references/*.md; do wc -c "$f"; done | sort -rn
# 2. 引用关系：孤儿 / 被引用数（孤儿≠该删，可能只是没挂链接）
grep -c "references/<名>" <skill_dir>/SKILL.md
# 3. 条目级定性：逐节看「这条是给谁看的」
grep -nE "^## " <refs>/<file>.md
```

**分类维度是「这条坑是谁的」，不是「历史/规程」**：实测 45 条里 35% 是「用这个 skill 时会撞上」（留 references）、
51% 是「改代码/验收时才撞」（移出到维护者档案）、14% 根本属别的 skill（移交正主）。
**按历史/规程二分会把「既是坑也是判据」的条目误分**——它们有 100+ 个判据词，却带着日期戳。

**清理的正确形态是换位置而非删内容**：references 留「用时表」判的，维护者档案留全文；
每条压成 ≤3 行时（只留「撞上什么 + 怎么办」，删掉根因分析与测出过程）**逐条给用户确认**，
压错一条 = 经验变难用。**压不动的就保持原样**——体量是次要成本，丢失经验是主要损失。

### 9. 修复后验证（必须，不验证不汇报）

- **同族病灶全库横扫**：修完一类问题（教学占位符假红、裸跨 skill 路径、含点文件名、说明句里复现旧字面）后，跑全库结构体检列出所有非绿 skill 再逐条定性——同类病灶极少只在一处，只修手里这一个 = 下一轮撞上同族再修一遍。横扫法（非绿才打印）：
  ```bash
  H=$(find ~/.hermes/skills -name audit_skill_health.py | head -1)
  for d in $(find ~/.hermes/skills -name SKILL.md -not -path '*/.archive/*' | xargs -n1 dirname); do
      out=$(python3 "$H" "$d"); [ $? -ne 0 ] && echo "$out"
  done
  ```
  扫出的每一项仍要三分类定性：真病（补/删）／假红（改口径或改措辞，不修内容）／工具 bug（改检查器，别改被检查的文件）。
- **改过检查器脚本本身 → 验证三件套（只跑正例等于没验）**：①**正例**（原本健康的对象仍绿）；②**负例夹具**（临时目录造一个：超限 description + 死 `triggers:` + 孤儿 references + 一个全库都不存在的断裂引用 + 与库内某 skill 同名的 `name`，跑完应全部红旗、退出码 1）；③**回归**（本轮改动涉及的正则/口径，拿原先全绿的对象复跑，确认没从「该红」变「哑」或「该绿变红」）。检查器改坏是**静默故障**——判据变松、发红变哑都不会自己冒出来：正例只证明「没把绿的整红」，证明不了「红的还能红」。**两条同族的假绿口径：**①**「全 flag 退出码 0」不是通过证据**——若退出码与内部 findings 脱钩（红字照打、退出码恒 0），那个 0 恒真，跑一百遍也验不出东西（实测：一个 skill 的四个检查 flag 全程 exit 0 且末行恒打「机械项通过」，唯独漏接收尾，闸门形同虚设）；判定「全 0」前先问「**该不该有非 0**」，每个会打红字的检查项都要单独确认「它红了之后退出码/结论行会变吗」，不会 → 那条红字对只看摘要的执行者是装饰，不构成闸门。②**「说了不许继续」却已经落了盘**——脚本报错/阻断时仍执行写盘、发请求、记日志，则那句结论是假的（副作用把下一轮的比对基准变成「本轮明确没做完」的状态）；副作用必须排在结论收口之后并受阻断条件拦截，**检验法：跑「失败场景 + 副作用」，比对文件 mtime/大小有没有变**。③**🔴 告警必须能改变结论（不进去就是装饰）**——某检测打出了醒目告警，但末行仍「✅ 通过」且退出码 0。**根因通常是接线：告警函数在检测点被调用，却没登记进决定结论的那个集合，而结论只读那个集合**。执行者只读末行时告警等于不存在。修法不是改措辞，是把告警登记进结论判定路径让它改变退出码；**分级纪律**：只有「继续会造成错误/数据损失」才用阻断级，「要求清理但不阻断」用注意级，两种都用阻断级会让真阻断信号淹没。**同一句打 N 遍**说明该函数被多处调用且无记忆化，收敛到一处（否则「吵了三次」会被读成三个不同问题）。夹具与断言**已固化成 `scripts/selftest.py`**（别再手搓一次性夹具：验完即弃 = 下一轮重造，且没人复跑就发现不了假绿；脚本一改先跑它）。
- **写夹具断言的两条铁律**（都属「断言与被测数据互相污染」，只跑一次不复跑时永远暴露不出来）：①**断言字符串必须是独有标记，不能是会被夹具内容自己命中的裸词**——实测断言 `"超限" in out`，而夹具的 description 文本里自带「超限」二字、被「前57」预览打出来 → 假通过；改成断言 `🔴 超限` 才有效。②**夹具必须真的触发该条件**——实测把 `orphan.md` 写进正文引用里，它就不再是孤儿，孤儿检测正确地沉默、断言反而「失败」；孤儿项要另放一个正文未提及的文件。用「先让断言全红、再逐条修绿」的顺序写夹具，比先写绿再验收据更早暴露这两条。
- **造测试数据前先读清被测逻辑的「真相来源」是哪一份**，否则会得到一个看似失败的假结论。实测：要验「基线漂移 → exit 2」，改了缓存文件（baseline.json）却始终 exit 0——因为被测逻辑以日志末条（history）为准，改缓存只触发「分叉」提示。**按字面猜哪份文件驱动判定，就会造出无效测法并把「没触发」误读成「有 bug」**。判据：动手造数据前先 grep 被测代码里 `base = A or B` 这类取值语句，确认改哪一份才真的改变判定。
- **造 fixture 前先确认「它能走到出问题的那个分支」（自写回归全绿的真正原因）**：**空 fixture 会让整套断言恒绿**——用空日志/空文件验证「有内容时的比对逻辑」，那个分支永远不执行，绿得毫无意义。实测：一条在真实数据上每次都触发的逻辑错误（日期粒度与秒级时间戳混用），因为夹具用空输入而完全无感，直到独立对抗验证才抓出。**判据：写完每条断言，回问「如果这行代码是坏的，我这条断言会红吗」；答不上来就是无效断言。** 自检法：临时把被测分支改坏一次，确认断言转红，再改回——**没转红的断言就是装饰**。
- 🔴 **故障注入：造一批「已知该被拦住」的输入，逐个看检查器报什么**（与上一条方向相反，两条都要做）：上一条防「断言恒绿」，本条防「检查器在失败路径上报绿」。实测在自写回归 14 项全绿时注入故障，三个静默放行同现——目标文件被清空（头部校验打 `✅ 全部条目头合法`，0 条 = 0 违规，末行 `exit 0「通过」`）、上限配 `0`（被 `max(x,1)` 吃掉 → 永远 100% 过线）、上限配负数（正则不匹配 → 静默回退默认 → 越线检查全失效）。**共同形态是「失败被下游当成成功」，按形态找比逐个 flag 试快。** 一条检查项的绿，含义是「**它测过会红**」而非「它跑过了」——前者只能靠注入担保。
- **刚写完的守卫立刻逐个残骸形态手工复测，不收工、不甩给子代理**：新守卫自己就有绕过路径，而你自己的清单里不会包含你没想到的形态。实测第一版判空守卫漏「只剩分隔符」与「只有 BOM」两种。**子代理测的是你给它的清单**——清单里没有的形态它同样漏；派单能补位不能代替。配套：**判空别直接复用现成的切分函数**（实测条目切分函数把 `'\ufeff'` 算成一条有效条目，因它非空且不以 `#` 开头）——先剥残骸再数；且**完整性要两条独立判据**（实测「有没有合格内容」只挡清零，62 条掉到 3 条、每条都完整全放行，需补一条**相对跌幅**判据且用跌幅不用绝对条数；新判据要接在「写回参照物」之前，否则坏状态会被继承成永久「一致」），**不能跨体裁照搬**（条格式判据用在散文体文件上会把 1,257 字的正常文件判成空）；**阈值类断言的覆盖只能靠变异测试证明**（实测 7 个用例对「阈值 16→4」变异全绿，因用例全在两侧远处、临界区间零覆盖）——把阈值改成一个**邻近合法值**看会不会红，见 [references/common-pitfalls.md](references/common-pitfalls.md)「刚写完的守卫」「判据要问『够不够像内容』」「判据不能跨体裁照搬」「「判有无」的守卫不拦「剩一点」」「「有 N 个用例」不证明阈值被测到」五节。
- **造数值类 fixture 必须按实测 len 反推，不能靠乘算**（量纲坑）：堆 N 条定不出目标占比，因为每条实长受编码/分隔符影响（实测按 60 字节算、实际 66，两档阈值撞在一起 → 判据恒失效）。做法：先造一条样本量出真实 `len`，再算条数，最后实测总量补差值到目标。**造完必须回读真实占比**，日志里打出来核对。
- **加强黑名单扫描器后必跑两个额外绿例**：①**扫含该扫描器自身的语料**（把 skill 目录 cp 到临时区跑）——模式表文件是违禁字面的最大来源（列 `<用户名>`/`ghp_`/密钥模式的文档自己就含这些字面），自咬恒红时给扫描器加 `--exclude` 模式权威文件清单，**不是删模式表**；②**扫一份真实干净语料**（已发布的通用化仓库/实际交付物）——命中即判据过宽，给显式占位符（`<your-key>`、`/Users/xxx`、`process.env` 读法）加行级豁免，别把假红留给执行者手改，误报会把闸门变成没人看的噪声。
- **断言写法两条卫生规则**：①「包含==相等」类断言（版本号、ID）必须带字符类词界 `grep -E "(^|[^0-9.])X([^0-9.]|$)"`——裸子串匹配让短值假过长值（传 `v2.7` 放行实际 `v2.7.7`，负例实测才暴露）；②验证脚本里「仅当条件满足才验」的分支，条件不满足时必须显式输出 ⚠️ 未验 + 给出人工补对命令，**禁止静默跳过**——否则「N 项全过」会在恰好没验过该项的情况下被当真收尾，静默的跳过比缺失的断言更危险，因为它披着已覆盖的外衣。
- 重新 grep 引用 vs 文件：全部引用存在、无孤儿
- 代码块配对为偶数
- 重复标题只剩一个
- frontmatter 无损（read 前 12 行确认 name/description/version 完好）
- 通读目标段确认无中间状态残留

### 9b. 有效性体检：别把「更诚实」当「更能干」

第 1–9 步查的都是**结构**（文件在不在、引用断没断、格式对不对）。用户问「改完真的更能干吗」时，要答的是另一件事——**这套检查/审计机制，在真实数据上到底抓不抓得到东西**。四个必答问题：

**1. 提删任何能力前，先查真实调用量，不看行数占比。** 体积占比 ≠ 价值：一份 700 行的检查器可能天天被调，一份 80 行的可能被完全没人用。查法（工具的调用记录在会话库里）：

```bash
# 从消息库统计该工具/flag 的真实调用次数与时间分布
sqlite3 ~/.hermes/state.db "select strftime('%Y-%m',timestamp,'unixepoch','localtime') m,
  count(*) from messages where tool_calls like '%<工具名>%' group by m;"
```

**时间分布比总次数更能说明问题**：全部调用集中在同一天、且会话标题都指向该工具自身的开发/验收 → 那是**开发调试**，不是真实使用，**不能作为「常用」的证据**。真实使用看的是「排除开发会话后，日常会话里还剩几次」。

**2. 报「检出率」前，逐条验证你植入的缺陷真的存在。** 拿真实缺陷当标准答案去测命中率，是测准率的最短路径——但**标准答案本身必须先核实**。造完「已知缺陷」清单后逐条 grep 真实数据：查无此事的条目要从分母里剔掉，否则你量的是一个不存在的靶子。**这一步同时是自我欺骗的照妖镜**：清单里若有「我以为存在」的缺陷，说明你并不掌握真实基线，此时任何命中率数字都不成立。

**3. 双副本一致性检查，对「同时更新两份」结构性失明。** 用两份文件互相校对以发现「谁被偷偷改了」的机制（如基线文件 vs 日志末条互校），**只能发现单边更新**。当一次操作同时改了两份（如正常写回流程一次追加两处），两份恒等，检查永远沉默——**而这恰恰是最需要被发现的情形**。判据：**报「无法检测某类改动」时要说明它是结构性的还是没查**，别让「检测器没报」被读成「没有这类改动」。

**4. 提改进方案时，分清这三条是互不替代的目标，别混着汇报**：

| 目标 | 含义 | 验证方式 |
|---|---|---|
| **检出率提升** | 原来抓不到的，现在能抓到 | 拿真实缺陷集量命中率（有分母） |
| **诚实度提升** | 声明与能力对齐，不再吹牛 | 比对声称 vs 实现 |
| **成本/体面** | 更短更快更整齐 | 量体积、耗时 |

**减法型改造（合并模式、剥日志、移文件）几乎只提升诚实度与体面，检出率零变化**——除非它顺带接上了原本没串进主流程的那一步。汇报时若只做了后两者，就说「诚实度/体面改善」，**不要答成「审计更能干了」**：用户问的若是后者，含糊其辞会让他以为问题解决了。反过来，**用户问「确定能到位么」而手上没有真实缺陷集时，正确答法是「不确定，缺的是漏报清单，跑一次真实任务才有」——先取数据，别先给方案。**

> 深度机制与实测反例见 [references/common-pitfalls.md](references/common-pitfalls.md)「有效性体检」节。

## 陷阱

> 实测坑表（patch 报 no_change / 删错位文件 / 未闭合代码块 / 假权威声称 / doc-script 割裂 / 8d 关键词盲区 / 外迁后旧引用残留等，勿在本句写死条数——8g 形态 4「宣称数≠实列数」自踩案 2026-09-22 实证）+ **体检后要不要跑路径模拟的判断框架** + **多轮修同一 skill 的验收方法**（自写回归不是证据、终止标准、按判据横切、「能用对」与「好用」两个验收面）→ [references/common-pitfalls.md](references/common-pitfalls.md)

### 多轮修同一 skill：第 2 轮起换验收方法（第 9 步的延伸）

自写回归是**照自己实现意图**造的用例，实现有偏差时用例与偏差同源，绿是恒真的——第 9 步三件套（正例/负例/回归）能验「检查器没被改坏」，**验不了「判据本身选得对不对」**。实测连修三轮自写回归全绿，独立对抗验证一轮抓 9 个、下一轮又抓 6 个。

**收尾前必须再走一轮独立对抗验证**（派子代理只找 bug 不修），派单写死三条：禁写真实系统（用 HOME 指向临时目录并**按完整依赖清单建齐文件**）／清 `__pycache__` 或带 `PYTHONDONTWRITEBYTECODE=1`（否则「读到新文件、跑的是旧行为」会误判修复无效）／发现被测文件中途被改要如实上报。

**「独立一轮没报出新问题」只在**一轮**成立**——实测连跑 5 轮每轮都报出真问题，规律是「每轮修的都对，但每轮都在边界/异常路径上开新洞」。**第 2 轮起改按「判据类型」横切**：把全部判据归成 N 类（退出码语义／副作用守卫／分级判据／计数口径／边界匹配／用法误用／可叠加性），每类跑正例+异常+误用三态，产出「已核/未核」清单。横切的产出是覆盖面结论（「10/13 类全绿」），强于「这次没撞上」。

**🔴 横切到 N 类全绿也不等于验完：还有一整层它验不到（实测 5 轮全绿后才被真实使用打脸）**。上述每一类验的都是「**输出是否符合声明**」——格式、口径、字段、退出码、边界行为。没有验的是「**声明本身对不对**」，而缺陷恰恰全在这一层：

| 层 | 验什么 | 谁负责 |
|---|---|---|
| 符合性 | 输出与声明是否一致 | 横切 / 独立对抗 / 回归用例 |
| **兑现性** | **声明承诺的东西，它真能兑现吗** | **只能靠真实使用** |

典型形态：脚本按设计吐出一个数字，**格式全对、字段全对、算法全对**，但那个词承诺的东西兑现不了——如输出「可压上限 N 字符」而它实际只是「处置对象体量」，差一个数量级，方向甚至会反。**任何静态或半静态验收都照不出来**：夹具造的是符合声明的输入，跑出来符合声明的输出，闭环自洽。

**判据：凡被测对象输出体量／上限／可用量／收益类数字，验收时必须做一次「拿这个数去兑现」的推演**——逐组问「这组真能省这么多吗，依赖什么前提，前提成立吗」。前提不成立（如「能否兑现取决于某项前置检查，而这一步流程里没串进来」）→ 该数字是**误导性产出**，属阻断级。

**因此收工标准要分层报，不能只说「验完了」**：
1. 横切给出**类目覆盖结论**（已核 N/M 类 + 各类证据）；
2. 单列**「声明层未验项」**——即「哪些产出没有被独立验证过能不能兑现」；
3. **真实使用是最强的兑现性验收面**：被测对象跑过一次完整真实任务，并拿实际结果对账每个关键数字——比对路径、报错/输出格式、耗时节流、与真实产物的差异（实测另一个会话用旧版跑完整任务，直接量出「候选池 4,310 字符、实际兑现 33 字符、方向反了」）。**发现差异先怀疑声明层，再查实现。**

**指标命名纪律**（改的时候连带做，通常比重算算法更有效）：**指标名先问「这个词会不会被下游读成承诺」**。「上限/总量/可回收」这类词会被直接当成目标数字，而它可能只是「待处置对象体量」——差一个量级就是误导。改法通常是**改名 + 显式标注三态**（这是上限 / 这是承诺 / 这是未知），让下游无法误读；数字本身可能不用改。配套：凡数字被放进预算表／容量账／报表旁，**每行都要标明它属于哪一类**，三类不能混在一张表里。

**🔴 5. 加固不等于对齐需求（多轮加判据时最易走偏的一条）**

连修多轮时，每次抓到 bug 就加一条判据，**方向感会静默漂移**：从「维护用户目标」滑成「让检查器更完备」。
**每加一条判据先回问「它回答的是用户真正在问的问题吗」**——实测偏了整整两轮：
用户要的是「**挑出该删的、该合并的**」（取舍清单），
而加的全是「文件坏没坏」（塌陷/断崖/容量/漂移）——**体检报告不是他要的交付物，**清单才是。
**机械判据防的是「漏看文件」，用户怕的遗漏是「该动的不动」，两者不是一件事。**

**这与第 4 条的「三目标不可混报」同源，但更隐蔽**：那条是汇报时混淆，这条是**改造方向本身跑偏**，
且因为每一步都在真抓真 bug、全程绿灯，**没有任何信号提示你跑偏了**。
**判据：连续多轮改造时，定期回到用户视角重述一句「这个工具交付什么」，
拿这句去对照本轮改动——对不上就是在给自己做无用功。**
**症状**：交付物形态没变（还是同一个 verdict 字符串），但内容量在涨。

**正确性之外另测一轮体感**（另一个验收面）：常用路径各跑一次，记耗时/输出行数/**首行是否回答「我要不要继续读」**——实测抓过「判决埋在 55 行最后一行，每次要读满才知道能不能继续」。完整判据与派单模板见 references/common-pitfalls.md「多轮修同一 skill：自写回归不是证据」节。

## 验证脚本

`<你的 skill 安装目录>/scripts/audit_skill_health.py` —— 自动执行第 2 步（孤儿+断裂引用）、第 4 步（重复标题）、第 5 步（代码块配对）三项机械检查，另含 frontmatter **存在性**检查（仅查 --- 开头；name/version/description 字段齐全性不查——第 9 步验证首项仍须人工 read 前 12 行，勿因脚本绿而跳过），传入 skill 目录即出报告。手动体检后跑一遍兜底。第 7/8 步（权威声称、兜底前向引用）需纯人工判断，脚本不覆盖；第 8g 步有候选提取器 `scripts/criteria_overlap.py`（四类 token 自动配对待账清单，见 8g 节），候选仍须人工定性。

`<你的 skill 安装目录>/scripts/preflight_check.py <skill_dir>` —— **开场预检一次跑完**：元信息（description 长度与超限、死 `triggers:` 字段、python len 字符数、references 估读量）、引用完整性（孤儿/断裂，占位名自动排除）、第 2b 步同名副本遮蔽、第 2c 步指路语（**强信号=真指路语逐条读 / 弱信号=仅编号枚举抽样**）、**并发写入提示**（`[并发写入]` 段：近 N 小时 skills 目录被哪些 actor 写过，开工前先看一眼防覆盖别人刚落的改动）、8g 下一步线索（`criteria_overlap` 命令 + 形态 3/6/7 的 lure 正则）。**替代十余条散装命令的往返与输出回灌**（2026-09-22 立）；退出码 0 = 无机械问题 / 1 = 有 / 2 = 用法错误。并发写入段按**时间窗口**过滤（`--window-hours`，默认 6），**不要改用 session_id**——桌面端会话轮转会换 id（实测同一对话 `050527_290c57` → `052612_275e22` 只差 30 毫秒），按 id 过滤会漏掉轮转后的写入；取证与处置细则见 skill-edit-safety 的坑表。

`preflight_check.py --scan-all` —— **全库门面扫描**（skill 数 / description 超限清单按字符数降序 / 死 `triggers:` 字段清单）。门面病灶常是全库同族（实测 139 个 skill 里十几个 description 超限，绝大多数是外部导入的，自家维护的也中过），逐个 `skill_dir` 跑看不出规模，一次扫完全库再按「自家 / 导入」分区决定修谁。

`<你的 skill 安装目录>/scripts/selftest.py` —— **两个脚本的自测**（无参数，夹具建在临时目录、跑完即删）：20+ 条断言覆盖五组真实踩坑——良构 skill 双绿且不被误报灯、病构 skill 双红并点名孤儿/断裂/重复二级标题/代码块未配对/description 越界、含点文件名不得被截断成 `08.md`、跨 skill 引用判 ℹ️ 非问题、`--scan-all` 抓到超限项。**改完任一脚本先跑它**；退出码 0 = 全过。发布版同捆，别人 clone 后也能自证（2026-09-22 立：此前每次改动都靠临时手搓夹具验证，验完即弃，本轮写它时立刻抓出两条假绿——断言词出现在夹具 description 里、「被引用的文件」不算孤儿）。

> **官方 linter**（`tools/skill_linter.py` 的 `lint_skill()`，补 frontmatter 与工具名口径）用法 + **规则假阳性边界**（`dangling-reference` 假阳性 100%、`shell-utility-reference` ~80%，私人 skill 有大量合法例外）→ [references/official-linter.md](references/official-linter.md)
